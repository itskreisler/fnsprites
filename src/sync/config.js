/**
 * @file config.js
 * @description Google Drive sync configuration.
 *
 * NOTE: OAuth web client IDs are public by design (they identify the app,
 * not authenticate it). This file must be committed so the GitHub Pages
 * deployment can load the GIS client. Keep this project's own Client ID;
 * do NOT replace it with a secret or server-side credential.
 */
export const DRIVE_CLIENT_ID = '714607383865-vhs1qs3g135epim62cp95s6enrtdmtvl.apps.googleusercontent.com';